image: debian:stretch

before_script:
  - apt-get update -qq && apt-get install -y -qq jq libmagic-dev make gcc

stages:
  - build
  - test

build:
  script:
    - make

test:
  script:
    # Test a few cases, to be grown
    - DIFF=1 ./test.sh 0:3

# TODO Test dynamic loading of libmagic
